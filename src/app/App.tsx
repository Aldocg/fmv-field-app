import {HashRouter,Navigate,Route,Routes} from 'react-router-dom';import {Toaster} from 'sonner';import {AuthProvider,useAuth} from '../features/auth/AuthContext';import {AppShell} from './AppShell';import {LoginPage} from '../pages/LoginPage';import {TodayPage} from '../pages/TodayPage';import {SchedulePage} from '../pages/SchedulePage';import {ClientsPage} from '../pages/ClientsPage';import {ExtrasPage} from '../pages/ExtrasPage';import {AccountPage} from '../pages/AccountPage';function Protected(){const{session,loading}=useAuth();if(loading)return <div className="grid min-h-screen place-items-center text-sm font-semibold text-slate-500">Loading FMV Field App...</div>;if(!session)return <LoginPage/>;return <Routes><Route element={<AppShell/>}><Route index element={<TodayPage/>}/><Route path="/schedule" element={<SchedulePage/>}/><Route path="/clients" element={<ClientsPage/>}/><Route path="/extras" element={<ExtrasPage/>}/><Route path="/account" element={<AccountPage/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Route></Routes>}export function App(){return <AuthProvider><HashRouter><Protected/></HashRouter><Toaster richColors position="top-center"/></AuthProvider>}
