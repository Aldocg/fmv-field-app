const days=['Wednesday','Thursday','Friday'] as const;export type ServiceDay=typeof days[number];export function DayTabs({value,onChange}:{value:ServiceDay;onChange:(d:ServiceDay)=>void}){return <div className="grid grid-cols-3 gap-1 rounded-2xl bg-slate-100 p-1">{days.map(d=><button key={d} onClick={()=>onChange(d)} className={`min-h-11 rounded-xl px-2 text-sm font-semibold ${value===d?'bg-white text-emerald-800 shadow-sm':'text-slate-600'}`}>{d.slice(0,3)}</button>)}</div>}
