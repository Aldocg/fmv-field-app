import {CalendarDays,ChevronRight,MapPin,MessageSquareText} from 'lucide-react';import {format,parseISO} from 'date-fns';import type {MonthlyServiceItem} from '../types/domain';import {normalizeStatus} from '../utils/status';import {StatusBadge} from './StatusBadge';export function ServiceCard({item,onOpen}:{item:MonthlyServiceItem;onOpen:(i:MonthlyServiceItem)=>void}){const a=[item.address_snapshot,item.city_snapshot].filter(Boolean).join(', ');const maps=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(a)}`;return <article className="rounded-3xl border border-slate-100 bg-white p-4 shadow-soft"><button onClick={()=>onOpen(item)} className="w-full text-left"><div className="flex justify-between gap-3"><div><h3 className="text-lg font-bold">{item.client_name_snapshot}</h3><p className="mt-1 flex gap-2 text-sm text-slate-500"><MapPin size={16}/>{a||'No address'}</p></div><ChevronRight className="text-slate-300"/></div><div className="mt-4 flex flex-wrap items-center gap-2 text-sm text-slate-600"><b>{item.service_name_snapshot}</b><span>•</span><span className="inline-flex items-center gap-1"><CalendarDays size={15}/>{item.scheduled_day.slice(0,3)} · {format(parseISO(item.scheduled_date),'MMM d')}</span>{item.observations&&<MessageSquareText size={16} className="text-emerald-700"/>}</div><div className="mt-4"><StatusBadge status={normalizeStatus(item.operational_status)}/></div></button><div className="mt-3 border-t border-slate-100 pt-3"><a href={maps} target="_blank" rel="noreferrer" className="inline-flex min-h-11 items-center gap-2 rounded-xl px-3 text-sm font-semibold text-emerald-800"><MapPin size={16}/>Open in Maps</a></div></article>}
