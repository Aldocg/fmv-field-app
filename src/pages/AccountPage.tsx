import {LogOut} from 'lucide-react';import {useAuth} from '../features/auth/AuthContext';export function AccountPage(){const{profile,user,signOut}=useAuth();const n=profile?.full_name||user?.email||'User';const initials=n.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]?.toUpperCase()).join('');return <div className="space-y-5"><header><p className="text-sm font-semibold uppercase text-emerald-700">Profile</p><h1 className="mt-1 text-3xl font-black">Account</h1></header><section className="rounded-3xl bg-white p-5 shadow-soft"><div className="flex items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-3xl bg-emerald-800 text-xl font-black text-white">{initials||'U'}</div><div><h2 className="text-xl font-bold">{n}</h2><p className="text-sm text-slate-500">{user?.email}</p></div></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-2xl bg-slate-50 p-3"><span className="text-slate-400">Role</span><p className="font-semibold">{profile?.role||'-'}</p></div><div className="rounded-2xl bg-slate-50 p-3"><span className="text-slate-400">Version</span><p className="font-semibold">0.1.0</p></div></div></section><button onClick={signOut} className="inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-2xl border border-rose-200 bg-white font-bold text-rose-700"><LogOut size={18}/>Sign Out</button></div>}
