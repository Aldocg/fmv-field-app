import {useState} from 'react';import {Leaf} from 'lucide-react';import {toast} from 'sonner';import {useAuth} from '../features/auth/AuthContext';export function LoginPage(){const{signIn}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{await signIn(email.trim(),password)}catch{toast.error('Invalid email or password.')}finally{setLoading(false)}}const recovery=import.meta.env.VITE_PASSWORD_RECOVERY_URL||'https://aldocg.github.io/Recovery/';return <main className="safe-top min-h-screen px-4 py-8"><div className="mx-auto max-w-md"><div className="mb-8 flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-800 text-white"><Leaf/></div><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-emerald-700">FMV</p><h1 className="text-xl font-black">LANDSCAPING LLC</h1></div></div><section className="rounded-[2rem] bg-white p-6 shadow-soft"><p className="text-sm font-semibold text-emerald-700">Field Service</p><h2 className="mt-1 text-3xl font-black">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to view and update the monthly service plan.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-semibold">Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required className="mt-2 min-h-12 w-full rounded-2xl border border-slate-200 px-3"/></label><label className="block text-sm font-semibold">Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required className="mt-2 min-h-12 w-full rounded-2xl border border-slate-200 px-3"/></label><button disabled={loading} className="min-h-12 w-full rounded-2xl bg-emerald-800 font-bold text-white">{loading?'Signing in...':'Sign In'}</button></form><a href={recovery} target="_blank" rel="noreferrer" className="mt-5 inline-flex min-h-11 items-center text-sm font-semibold text-emerald-800">Forgot password?</a></section></div></main>}
